#################################################
# DEVELOPMENT Server Profile - Transfer         #
#################################################

## Application configurations:
server:
   port: 8081
   servlet:
      context-path: /bukopinmobile-transfer


## Database configuration:
#spring.jpa.hibernate.ddl-auto=create
spring:
   datasource:
      url: jdbc:postgresql://10.0.24.42:5432/DB_MBANKING 
      username: postgres
      password: Bukopin10@)!)
      driver-class-name: org.postgresql.Driver
   jpa:
      hibernate:
         ddl-auto: update      
         #spring.jpa.hibernate.ddl-auto=none
      properties:
         hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
         hibernate.jdbc.lob.non_contextual_creation: true

## LIQUIBASE (LiquibaseProperties)
# spring.liquibase.enabled=false

## LOGGING
logging:
   level:
      root: ERROR
      id.co.asyst: TRACE

jwt:
   header: Authorization
   
## CONFIG
config:
   max:
      most.frequent: 3 # maximum size of most frequent list result 
      receiver:
         overbook: 25 # maximum size of favourite overbook list result
         transfer: 25 # maximum size of favourite transfer list result

## IP CONFIG
   ip:
      passtrough: # client ip no need token
         10.2.62.163, 
         0:0:0:0:0:0:0:1,
         127.0.0.1
   header:
      overbook:
         api-key: 77474043-b7a0-489f-81c5-387ae027f8aa
            
## url for other module
url:
   masterdata:
      configuration: http://10.0.13.61:8082/
   user: http://10.0.13.61:8080/